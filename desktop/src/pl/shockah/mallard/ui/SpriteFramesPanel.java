package pl.shockah.mallard.ui;

import javax.annotation.Nonnull;

import pl.shockah.godwit.State;

public class SpriteFramesPanel extends MallardPanel {
	public SpriteFramesPanel(@Nonnull State state) {
		super(state);
	}
}